spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gumroad
    username: rakshit
    password: rakshit
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
    hibernate:
      ddl-auto: validate
